spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration

  kafka:
    consumer:
      group-id: quote-flow-service-test
      auto-offset-reset: earliest

app:
  outbox:
    publisher-fixed-delay-ms: 99999999
